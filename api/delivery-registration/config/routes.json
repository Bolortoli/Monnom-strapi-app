{
  "routes": [
    {
      "method": "GET",
      "path": "/delivery-registrations",
      "handler": "delivery-registration.find",
      "config": {
        "policies": []
      }
    },
    {
      "method": "GET",
      "path": "/delivery-registrations/count",
      "handler": "delivery-registration.count",
      "config": {
        "policies": []
      }
    },
    {
      "method": "GET",
      "path": "/delivery-registrations/:id",
      "handler": "delivery-registration.findOne",
      "config": {
        "policies": []
      }
    },
    {
      "method": "POST",
      "path": "/delivery-registrations",
      "handler": "delivery-registration.create",
      "config": {
        "policies": []
      }
    },
    {
      "method": "PUT",
      "path": "/delivery-registrations/:id",
      "handler": "delivery-registration.update",
      "config": {
        "policies": []
      }
    },
    {
      "method": "DELETE",
      "path": "/delivery-registrations/:id",
      "handler": "delivery-registration.delete",
      "config": {
        "policies": []
      }
    }
  ]
}
